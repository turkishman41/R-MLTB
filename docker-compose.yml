version: "3.3"

services:
  app:
    build: .
    container_name: rcmltb
    command: bash start.sh
    volumes:
      - /usr/src/app/downloads:/usr/src/app/downloads:rw # optional
    ports:
      - "11:11" # qbittorrent selection webserver
      - "1111:1111" # rclone serve index webserver
    restart: on-failure
      
      
